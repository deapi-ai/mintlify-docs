---
title: "Image OCR"
openapi: "/openapi-v2.json POST /api/v2/images/ocr"
---

Extract text from images (Optical Character Recognition). Returns a `request_id` for status polling.

<Note>
**Prerequisite:** Consult the [Model Selection](/api/v2/utilities/models) endpoint to identify a valid model `slug`.
</Note>
