---
title: "Upload Video File (Video-to-Text)"
api: "POST https://api.deapi.ai/api/v1/client/videofile2txt"
---

Transcribe uploaded video files.

**Prerequisite:** Consult the [Model Selection](/api/model-selection) endpoint to identify a valid model `slug`.

<ParamField header="Authorization" type="string" required>
  Bearer token. Format: `Bearer YOUR_API_KEY`
</ParamField>

<ParamField body="model" type="string" required>
  Model slug from Model Selection endpoint.
</ParamField>

<ParamField body="video" type="file" required>
  Video file to transcribe.
</ParamField>

<ParamField body="include_ts" type="boolean" default="false">
  Include timestamps in transcription.
</ParamField>

---

## Calculate Price

**Endpoint:** `POST https://api.deapi.ai/api/v1/client/videofile2txt/price-calculation`

```bash
curl --request POST \
  --url https://api.deapi.ai/api/v1/client/videofile2txt/price-calculation \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: multipart/form-data' \
  --form 'model=transcription-model'
```
