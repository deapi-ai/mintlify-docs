---
title: "Video Upscale"
openapi: "/openapi.json POST /api/v1/client/vid-upscale"
---

<Note>
**Prerequisite:** To ensure a successful request, you must first consult the [Model Selection](/api/utilities/model-selection) endpoint to identify a valid model `slug` and check specific **limits** (including supported `scale` range and maximum input resolution/duration).
</Note>